<Window x:Class="LoginClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="250" Width="380"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="90" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Content="Username:" />
        <TextBox x:Name="UsernameTextBox" Grid.Row="0" Grid.Column="1"
                 Margin="8,0,0,12" MinWidth="200" />

        <Label Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Content="Password:" />
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1"
                     Margin="8,0,0,12" />

        <Button x:Name="LoginButton" Grid.Row="2" Grid.Column="1"
                Width="100" HorizontalAlignment="Right" Content="Login"
                Click="LoginButton_Click" />
    </Grid>
</Window>
